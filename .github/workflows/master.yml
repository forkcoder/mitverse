on:
  # workflow_call:
  #   secrets:
  #     username:
  #       description: 'The username to access ftp server'
  #       required: true
  #     password:
  #       description: 'The password to access ftp server'
  #       required: true 
  #     server-dir:   
  #       description: 'The location of ftp storage'
  #       required: true 
  push:
    branches:
      - main
name: 🚀 Mitverse
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.forkcoder.com
        port: 21
        server-dir: ${{ secrets.ftp_location }}
        username: ${{ secrets.ftp_username }}
        password: ${{ secrets.ftp_password }}