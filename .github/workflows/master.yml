on: 
  push:
    branches:
      - main
  workflow_call:
    secrets:
      ftp_username:
        description: 'The username to access ftp server'
        required: true
      ftp_password:
        description: 'The password to access ftp server'
        required: true  
name: 🚀 Mitverse
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.forkcoder.com
        username: ${{ secrects.ftp_username }}
        password: ${{ secrects.ftp_password }}
        server-dir: /repositories/mitverse/
        port: 21